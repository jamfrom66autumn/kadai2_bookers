<h1>Editing Book</h1>
<%= form_with model:@book, url:"/books/#{list.id}", local:true do |f|%>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>
    <%= f.%>
    <%= f.%>
<% end %>
